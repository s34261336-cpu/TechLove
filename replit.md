# TechLove

Импортированный Python-бот с AI-ассистентом в Telegram, выбором моделей и ролей, историей диалогов и административными инструментами.

## Запуск

- Рабочий процесс **TechLove Bot** запускает `python app.py`.
- Данные пользователей, моделей и кейсов хранятся в JSON-файлах в корне проекта.
- Для работы нужны секреты `BOT_TOKEN` и `GROQ_API_KEY`. Для моделей SeekAI также нужен
  секрет `SEEKAI_API_KEY`; адрес API задаётся через `SEEKAI_BASE_URL` и по умолчанию
  использует `https://seekai.cc`.

## Стек

- Python 3.12
- aiogram 3
- OpenAI-совместимые AI API: Groq и SeekAI
- aiohttp
- JSON-файлы для хранения состояния

## Возможности

- Несколько AI-моделей и ролей ассистента
- Распознавание голосовых сообщений локальной моделью Whisper
- История диалогов и настройки температуры
- Генерация и анализ изображений
- Профиль пользователя и система ZenoToken
- Антиспам-защита
- Административная панель с управлением пользователями, моделями и рассылками

## Импорт

Исходники импортированы из `https://github.com/s34261336-cpu/TechLove.git`.

## Пользовательские настройки

Проект сохранён как Python-приложение с запуском через кнопку Run.
